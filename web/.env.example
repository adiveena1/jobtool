# Server-only. Never prefix with NEXT_PUBLIC_ — that would ship it to the browser.
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-5

# Absolute origin, used for Open Graph URLs.
SITE_URL=http://localhost:3000

# Set to "true" to make identify() reject callers without a session.
REQUIRE_AUTH=false
